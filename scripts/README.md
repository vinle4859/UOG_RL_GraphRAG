# =============================================================================
# scripts/README.md
# One-off utility scripts.
# =============================================================================
# Place standalone scripts here (data migration, one-time processing, etc.).
# These are NOT part of the main package — they're run directly.
#
# Example:
#   python scripts/export_graph_stats.py
